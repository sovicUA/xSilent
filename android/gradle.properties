org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental compilation спричиняє "Could not close incremental caches"
# на Windows (блокування .tab-файлів антивірусом / Build Tools API). Вимкнено —
# для цього проєкту (три невеликі Kotlin-модулі плагінів) виграшу від нього нема.
kotlin.incremental=false
