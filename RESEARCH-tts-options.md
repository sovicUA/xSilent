# Дослідження: сучасний синтез мовлення (TTS) для озвучення

Робочий документ для подальшого вивчення. Дата: 2026-09-09. Область: **Android**.
Пов'язане: [PLAN-notification-types.md](PLAN-notification-types.md) (вибір голосу
вже реалізовано через системний рушій), [DECISIONS.md](DECISIONS.md).

## Проблема

Системні рушії дають «електронне», неприродне звучання:

- **RHVoice** (Anatol / Volodymyr / Marianna / Natalia) — стара технологія
  (HMM/склейка), звучить механічно; фонемізатор espeak-ng плутає укр./рос.
  літери. Єдиний плюс — є чоловічі голоси. Користувач: «страшно електронно».
- **Google TTS `uk-ua-x-hfd-local`** (24 кГц) — старий конкатенативний голос,
  теж роботизований. Це те, що чує більшість.
- **eSpeak-NG** — ще гірше.

**Питання:** чи є сучасніший (нейромережевий / «AI») синтез і яка складність
його інтеграції.

## Що є зараз

`AnnouncementService` → `flutter_tts.synthesizeToFile` → **системний рушій за
замовчуванням** → WAV → запікається у звук каналу сповіщення (кешується, далі
грає й офлайн). Голос обирається глобально (`tts_voice`), входить у хеш каналу.

Ключове архітектурне: **синтез відбувається один раз** (при збереженні
нагадування / зміні голосу), результат — файл. Тож **одноразовий онлайн-синтез
архітектурно прийнятний** (мережеві голоси Google вже так працюють).

---

## Варіант A. Системні рушії (offline, безкоштовно, 0 інтеграції)

### A1. Google TTS — нейронний голос ⭐ швидкий виграш

Новіші голоси Google — **нейромережеві** (SeaNet / `uk-ua-x-multi`), суттєво
кращі за `hfd-local`. На тестовому пристрої (Lenovo TB305XU, Android 15):

| Голос у списку | Реальний рушій | Частота | Технологія |
|---|---|---|---|
| `uk-ua-x-hfd-local` | конкатенативний | 24 кГц | стара, роботизована |
| `uk-ua-x-hfd-network` | `uk-ua-x-hfd-seanet-embedded` | **48 кГц** | **нейронна** |
| `uk-UA-language` | маршрутизується на seanet | 24 кГц | нейронна (?) |

Нейронний голос (`hfd-network`) **безкоштовний**, синтезується онлайн один раз,
далі кешований WAV грає офлайн. Мінуси: лише **жіночий**, один тембр, перший
синтез потребує інтернету.

**Дія:** у списку голосів поставити нейронний першим із позначкою «рекомендовано»,
низькоякісні (`hfd-local`, RHVoice) — нижче або приховати. Оцінка: **0.5 дня**.
Можливо, цього достатньо — зразки для оцінки додано (див. кінець).

### A2. RHVoice — відхилити

Встановлено на тестовому планшеті. Якість неприйнятна (див. проблему).
Лишити доступним у списку (для тих, кому потрібен саме чоловічий офлайн-голос),
але не рекомендувати.

---

## Варіант B. Хмарний AI-синтез (мережа, висока якість)

Синтез один раз онлайн → кеш → офлайн-відтворення збережено.

### B1. Microsoft Azure Speech ⭐ найкраще співвідношення якість/зусилля

- Голоси: **`uk-UA-PolinaNeural`** (♀), **`uk-UA-OstapNeural`** (♂) — обидві
  статі, природне нейронне звучання. Є й HD-голоси.
- **Free tier F0: 500 000 символів/міс** нейронних. Далі — $16/1 млн.
  Реалістичне навантаження застосунку — сотні символів на нагадування,
  синтезується раз → **фактично безкоштовно назавжди**.
- API: REST (`POST https://<region>.tts.speech.microsoft.com/cognitiveservices/v1`,
  SSML у тілі, `Ocp-Apim-Subscription-Key` у заголовку, повертає аудіо). ~60 рядків Dart, без SDK.
- Регіональний endpoint + ключ.

### B2. Google Cloud Text-to-Speech

- Голоси uk-UA: **`uk-UA-Standard-A`** (♀), **`uk-UA-Wavenet-A`** (♀) — додані
  ~квітень 2025. WaveNet = deep learning, добре (не топ). **Neural2 / Chirp 3 HD
  для uk немає.** Лише **жіночі**.
- Free: 4 млн/міс Standard, 1 млн/міс WaveNet. $4 / $16 за 1 млн.
- API: REST + API-ключ. Просто.

### B3. ElevenLabs — найвища якість, дорожче

- Multilingual v2/v3 підтримує укр. Топова природність, емоції, клонування голосу.
- $0.10/1К символів (v2), $0.05/1К (Flash). **Free tier лише ~10К символів/міс.**
  Платні від $5–6/міс (~30К символів). API-ключ.
- Для цього застосунку — надлишок; менша безкоштовна квота.

### B4. OpenAI TTS (`gpt-4o-mini-tts` / `tts-1`)

- Укр. підтримується (мультимовна модель). Якість добра, іноді легкий чужий
  акцент/просодія. Голоси не україномовні за походженням (alloy, nova…),
  керовані промптом.
- ~$12–15 за 1 млн символів. Без вагомого free tier. API-ключ.

### B5. Amazon Polly

- `uk-UA` **«Kateryna»** (нейронний, ♀, ~2023). Free 1 млн/міс нейронних перші
  12 міс, далі $16/1 млн.
- Auth складніша (AWS SigV4). Лише жіночий.

### Спільні мінуси хмарних варіантів

- **Текст залишає пристрій** — суперечить позиціонуванню «повністю офлайн, нічого
  не передаємо». Пом'якшення: опційно (лише якщо користувач обрав хмарний голос),
  передається лише текст оголошення (який користувач сам увів), синтез один раз.
  Потрібне **оновлення політики приватності + форми Data safety** в Play.
- **API-ключ у застосунку** можна дістати з APK. Ризик зловживання чужим
  трафіком. Пом'якшення: обмеження ключа (лише TTS API + підпис застосунку —
  для Google/Azure є такі обмеження), або проксі (ламає «без бекенду»). За
  мізерного обсягу ризик грошей малий, але ключ варто вміти швидко ротувати.

---

## Варіант C. On-device нейромережеві моделі (offline, безкоштовно, важко)

### C1. sherpa-onnx + Piper/VITS ⭐ якщо офлайн — принципово

- **`sherpa_onnx`** (pub.dev, k2-fsa) — програє VITS / Piper / Kokoro ONNX-моделі
  **повністю офлайн**. Готові Android-бібліотеки (arm64/armv7/x86_64), Dart API,
  активна підтримка. Є демо-застосунки (VoxSherpa).
- Українська: Piper **`uk_UA-ukrainian_tts-medium`** (VITS, дані robinhad/egorsmkv)
  або `uk_UA-lada-x_low`. k2-fsa публікує конвертовані моделі (`vits-piper-*`).
- **Розмір:** модель medium ~60 МБ + onnxruntime .so ~15–25 МБ/ABI →
  **+80–120 МБ** до застосунку. Рекомендовано — окремий пак для завантаження
  (як робить RHVoice), не в бандлі.
- **Якість:** помітно краще за RHVoice — природніша просодія; але є огріхи G2P
  (наголоси, числа, скорочення — фонеми через espeak-ng). Модель `ukrainian_tts`
  краща за `lada`.
- **Складність: ВИСОКА.** Нативні бібліотеки, хостинг + UX завантаження моделі,
  окрема гілка синтезу поряд з `flutter_tts` (або заміна), обробка фонем. iOS
  пізніше — ще більше роботи.

### C2. MMS-TTS (Meta) `facebook/mms-tts-ukr`

- VITS, ~40 МБ, конвертується в ONNX. Якість — прийнятна, монотонніша за Piper.
- Складність — як у C1 (через sherpa-onnx чи onnxruntime напряму).

### C3. Kokoro-82M

- Відмінна якість, ONNX, працює on-device через sherpa-onnx. **Українську
  офіційно не підтримує** (станом на зараз). Відкласти для uk.

---

## Порівняльна таблиця

| Варіант | Якість | Offline | Вартість | Складність | Стать | Примітка |
|---|---|---|---|---|---|---|
| A1 Google нейронний | 🟢 добре | після 1-го синтезу | 0 | **0.5 дн** | лише ♀ | швидкий виграш |
| A2 RHVoice | 🔴 погано | ✅ | 0 | 0 (є) | ♀ + ♂ | відхилено |
| B1 **Azure** | 🟢🟢 дуже добре | після 1-го синтезу | ~0 (free 500K/міс) | **2–3 дн** | ♀ + ♂ | найкраще QER |
| B2 Google Cloud | 🟢 добре (WaveNet) | після 1-го синтезу | ~0 (free 1–4M/міс) | 2 дн | лише ♀ | |
| B3 ElevenLabs | 🟢🟢🟢 топ | після 1-го синтезу | $5–6/міс+ | 2–3 дн | будь-яка | дорого, мала квота |
| B4 OpenAI | 🟢 добре | після 1-го синтезу | ~$13/1M | 2 дн | не-укр. голоси | |
| C1 sherpa-onnx + Piper | 🟡 середньо-добре | ✅ повністю | 0 | **4–6 дн** | залежить від моделі | +80–120 МБ |
| C2 MMS-TTS | 🟡 середньо | ✅ повністю | 0 | 4–6 дн | ♀ | +40 МБ |

---

## Вплив на архітектуру (для B*)

1. **Політика приватності** (`store/privacy-policy.md` + `docs/privacy-policy/`)
   — додати абзац: «якщо обрано хмарний голос, текст оголошення один раз
   надсилається до <провайдера> для синтезу; аудіо зберігається на пристрої».
2. **Data safety** у Play Console — позначити передачу тексту (опційну).
3. **Мережа** — застосунок вперше отримує `INTERNET` як фактично використаний
   дозвіл (зараз у release-маніфесті його немає). Додати.
4. **Кешування** — без змін: результат синтезу (хмарного чи локального) однаково
   стає WAV → канал. Хеш каналу вже включає ім'я голосу.
5. **Офлайн-fallback** — якщо хмарний синтез не вдався (немає мережі), падати на
   системний нейронний голос (A1) + показувати підказку; при наступному
   `reconcile()` онлайн — пере-синтез.
6. **`AnnouncementService`** — ввести абстракцію «джерело синтезу»:
   `SystemTts` (є) / `AzureTts` / `SherpaTts`. `_speech()` обирає за типом голосу.

---

## Рекомендація

1. **Спершу A1 (0.5 дн)** — переставити список так, щоб нейронний Google був за
   замовчуванням; оцінити, чи його якість прийнятна (зразки нижче). Часто цього
   достатньо, бо проблема саме в RHVoice/`hfd-local`.
2. **Якщо потрібні «AI-голоси» й обидві статі — B1 (Azure), 2–3 дн.** Найкраще
   співвідношення якість/зусилля/ціна. Опційний, поряд із безкоштовними
   системними голосами. Потребує оновлення політики приватності.
3. **C1 (sherpa-onnx) — лише якщо повний офлайн принциповий** і Azure-компроміс
   із приватністю неприйнятний. 4–6 дн, +100 МБ, якість нижча за Azure.
4. **B3/B4/B5** — не пріоритет (дорого / мала квота / складна auth / лише ♀).

Порядок з користувачем: спочатку оформити це дослідження (цей файл) →
потім оцінити зразки → рішення.

---

## Аудіозразки (текст: «Це зразок обраного голосу озвучення.»)

Знято на Lenovo TB305XU (кнопка «прослухати зразок» у налаштуваннях).
Порівняння (програти в браузері):
**https://claude.ai/code/artifact/84c170b2-c142-4ae4-92ae-0b7b0174c36e**
Файли — `scratchpad/tts-samples/` (не в репо).

| Файл | Голос | Технологія |
|---|---|---|
| `01-google-hfd-local` | Google `uk-ua-x-hfd-local` | стара конкатенативна, 24 кГц |
| `02-google-hfd-network` | Google `uk-ua-x-hfd-network` (seanet) | **нейронна, 48 кГц** |
| `03-rhvoice-natalia` | RHVoice Natalia ♀ | HMM/склейка |
| `04-rhvoice-anatol` | RHVoice Anatol ♂ | HMM/склейка |

Хмарні (Azure/ElevenLabs) зразки — зробити окремо через їхні демо/API за потреби.

---

## Джерела

- Azure Speech — мови/голоси: https://learn.microsoft.com/azure/ai-services/speech-service/language-support ; ціни: $16/1M нейронні, free F0 500K/міс.
- Google Cloud TTS — `uk-UA-Standard-A` / `uk-UA-Wavenet-A` (~квітень 2025); ціни https://docs.cloud.google.com/text-to-speech
- ElevenLabs — https://elevenlabs.io/pricing/api ($0.10/1K симв. v2)
- OpenAI TTS — https://developers.openai.com/api/docs/guides/text-to-speech
- Amazon Polly — `uk-UA` Kateryna (нейронний)
- sherpa-onnx — https://pub.dev/packages/sherpa_onnx ; моделі https://github.com/k2-fsa/sherpa-onnx
- Piper голоси — https://github.com/rhasspy/piper/blob/master/VOICES.md (`uk_UA` lada x_low / ukrainian_tts medium)
- robinhad/ukrainian-tts — https://github.com/robinhad/ukrainian-tts
